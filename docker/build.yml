version: "3.8"
services:
    jetson-app:
        image: ${ARCH}dsgn2:${TAG}
        build:
            context: ${PRODUCTION_PATH}
            dockerfile: Dockerfile.x86_64
        container_name: dsgn2
        tty: true
